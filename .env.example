# Used by docker-compose.yml for variable substitution (`docker compose up`
# reads .env at the project root automatically). Not needed if you're running
# apps/server directly with npm — use apps/server/.env for that instead.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:4000/api/youtube/callback
